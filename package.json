{
  "private": false,
  "name": "@supervise-dev/bridge",
  "version": "0.1.5",
  "description": "A tRPC-based bridge for filesystem and process operations with both library and server components",
  "scripts": {
    "build": "tsc && bun build src/client.ts --outdir ./dist --target node --sourcemap=external",
    "postinstall": "pnpm run build",
    "build:bridge": "bun build src/index.ts --compile --outfile ./dist/bridge --target bun",
    "build:bridge:darwin-arm64": "bun build src/index.ts --compile --outfile ./dist/bridge-darwin-arm64 --target bun-darwin-arm64",
    "build:bridge:darwin-x64": "bun build src/index.ts --compile --outfile ./dist/bridge-darwin-x64 --target bun-darwin-x64",
    "build:bridge:linux-arm64": "bun build src/index.ts --compile --outfile ./dist/bridge-linux-arm64 --target bun-linux-arm64",
    "build:bridge:linux-x64": "bun build src/index.ts --compile --outfile ./dist/bridge-linux-x64 --target bun-linux-x64",
    "format": "prettier --write \"src/**/*.ts\"",
    "lint": "eslint src",
    "typecheck": "tsc --noEmit"
  },
  "bin": {
    "bridge": "dist/bridge"
  },
  "main": "./dist/client.js",
  "types": "./dist/client.d.ts",
  "files": [
    "dist/client.js",
    "dist/client.d.ts",
    "dist/client.js.map",
    "dist/fs",
    "dist/process",
    "dist/router.d.ts",
    "dist/schema.d.ts",
    "dist/index.d.ts",
    "src",
    "tsconfig.json"
  ],
  "dependencies": {
    "@trpc/client": "npm:@trpc/client",
    "@trpc/server": "npm:@trpc/server",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/bun": "^1.3.1"
  },
  "publishConfig": {
    "registry": "https://npm.pkg.github.com"
  }
}
